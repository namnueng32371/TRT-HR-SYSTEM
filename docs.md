ถ้าจะรัน dev mode ต้องเปิด 2 อย่าง
-รัน npm run dev ใน bash vite
-รัน php artisan serve ใน bash run backend  ***ต้องไปเปิด XAMPP ด้วย

ข้อมูลเรื่อง Database โดยตารางหลักคือ emplyees 
โดย primary key คือ id ของตาราง emplyees โดยตารางอื่นๆจะใช้ Foreign Key โดยมีชื่อ employee_id ที่เป็นตัวเชื่อมกลับมาหา id ของพนักงาน


